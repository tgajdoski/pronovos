import { ThumbImageDirective } from './thumb-image.directive';

describe('ThumbImageDirective', () => {
  it('should create an instance', () => {
    const directive = new ThumbImageDirective();
    expect(directive).toBeTruthy();
  });
});
